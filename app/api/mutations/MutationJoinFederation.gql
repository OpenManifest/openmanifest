mutation JoinFederation($federationId: Int!, $uid: String, $licenseId: Int) {
  joinFederation(input: { attributes: { federationId: $federationId, uid: $uid, licenseId: $licenseId }}) {
    user {
      id
      name
      nickname
      userFederations {
        federation {
          id
          name
          slug
        }
        license {
          id
          name
        }
      }
    }
  }
}
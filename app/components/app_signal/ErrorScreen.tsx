import * as React from 'react';
import { Title } from 'react-native-paper';
import ScrollableScreen from '../layout/ScrollableScreen';

export default function ErrorScreen() {
  return (
    <ScrollableScreen>
      <Title>Something went wronng</Title>
    </ScrollableScreen>
  );
}
